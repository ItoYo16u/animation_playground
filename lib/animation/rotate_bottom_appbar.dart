// controller(lowwer_bound:0,upperbould:2),
// idx = [0,1,2]
// if current.idx < target.idx
// controller.forward(idx)
// else controller.backTo(idx);

// animation1 : rotate Tween<double> [0,math.pi*2]
// animation2 : move position
// button with clippath